# BizRanker Ansible

Ansible will use the realm inventory as the canonical host source:

../realm/inventory/hosts.yml

Planned roles:
- realm_base
- realm_catalog_agent
- nginx_ui_node
- postgres_node
- scraper_worker_node
- core_demo_node
