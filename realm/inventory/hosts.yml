all:
  children:

    core_nodes:
      hosts:
        bizranker-core:
          role: core_demo_node

    ui_nodes:
      hosts:
        bizranker-web-01:
          role: ui_node

    db_nodes:
      hosts:
        bizranker-db-01:
          role: postgres_node

    worker_nodes:
      hosts:
        bizranker-worker-01:
          role: scraper_worker_node
